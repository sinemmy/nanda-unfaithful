# Core dependencies
PyYAML
python-dotenv
beautifulsoup4
lxml
tqdm
numpy

# ML dependencies
# Note: torch should be installed separately with CUDA support
# Run: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
transformers
accelerate
bitsandbytes
sentencepiece
tokenizers

# Datasets
datasets

# vast.ai management (optional)
vastai