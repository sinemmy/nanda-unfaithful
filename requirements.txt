# Core dependencies
PyYAML
python-dotenv
beautifulsoup4  # For parsing model outputs with HTML-like tags
lxml            # Parser for BeautifulSoup

# API client libraries - NOT NEEDED for HuggingFace approach
# Only uncomment if you want to run Anthropic's original API-based experiments
# anthropic
# openai
# google-generativeai

# For HuggingFace model loading (Qwen-14B)
torch
transformers
accelerate
datasets
sentencepiece  # Required for some tokenizers
tokenizers     # Fast tokenizers for transformers

# Additional utilities
tqdm
numpy

# vast.ai management (optional but recommended)
vastai  # CLI for managing vast.ai instances